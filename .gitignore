node_modules/
dist/
*.js.map
*.d.ts.map
CLAUDE.md

# Secrets — never commit. See .env.example and `npm run auth`.
.env
.env.*
!.env.example
token.json
token.json.tmp
*.local.json
.claude/settings.local.json

# Ignore .vscode except the (secret-free) MCP launch config.
# Note: must exclude the directory contents, not the directory itself,
# for the negation below to take effect.
.vscode/*
!.vscode/mcp.json
