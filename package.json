{
  "name": "google-health-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Google Health API integration - access fitness, health metrics, sleep, nutrition, and device data",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "google-health-mcp-server": "dist/index.js"
  },
  "scripts": {
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "clean": "rimraf dist",
    "auth": "npm run build && node dist/cli/login.js",
    "auth:status": "npm run build && node dist/cli/status.js",
    "test": "node --import tsx --test src/constants.test.ts src/formatters.test.ts src/api-client.test.ts src/tools/shared.test.ts src/auth/store.test.ts src/auth/oauth.test.ts",
    "check-secrets": "node scripts/check-secrets.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "axios": "^1.7.9",
    "google-auth-library": "^9.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
